//-объявляем блок переменных, он не виден на странице
block variables

<!DOCTYPE html>
html(lang="ru")
    head
        meta(charset="UTF-8")
        //-"ширина устройства, пересёч при изменении viewport" == "width=device-width, initial-scale=1.0"
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        //-заменить на стили для каждой страницы
        link(rel="stylesheet", href="/assets/styles/app.min.css") 
        //-будем подставлять сюда заголовки #(title) для каждой страницы
        title #{title}
        //- script(async, defer, src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDPNNwBKiezkzEec7PFI6X3A-5xNHA6dJ4&callback=initMap", type="text/javascript")

    body
        .wrapper
            block content
            //-вставка из каждого файла
            
            include common/menu.pug

        //- script(async defer src="https://maps.googleapis.com/maps/api/js?callback=initMap") 